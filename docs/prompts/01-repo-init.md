# 01 — Repo Initialization Prompt

**Use**: Once, at the start of a new session or when onboarding a new agent.

---

You are working on MediStock, a Flutter Android pharmacy inventory MVP with
a NestJS backend and PostgreSQL.

Before doing anything:
1. Read /AGENTS.md — those rules are mandatory.
2. Read /docs/README.md — that's the index of source-of-truth docs.
3. Read /docs/prd.md and /docs/folder_structure.md in full.

Vertical slices are defined in docs/folder_structure.md (Slices 0–10). Do not
start coding before confirming which slice we are on. Do not skip slices.
Do not add features outside the active slice's scope.

Confirm you've read the above by replying with:

> Repo rules loaded. Current slice: `<X>`. Ready for your instruction.
